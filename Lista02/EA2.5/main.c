#include <stdio.h>
#include <stdlib.h>

int main()
{
    float x, y, diferenca;
    scanf("%f%f", &x, &y);
    diferenca = x - y;
    printf("A diferenca entre eles eh de %f\n", diferenca);
    return 0;
}
